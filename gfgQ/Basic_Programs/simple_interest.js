// Python Program for simple interest

// Simple interest formula is given by:
// Simple Interest = (P x T x R)/100
// Where,
// P is the principle amount
// T is the time and
// R is the rate

const getSinterest = (P,T,R) => {return ((P * T * R)/100)}

console.log(getSinterest(10000, 5, 5));